# Document manipulation, used by every skill in this repository
python-docx>=1.1.0
python-pptx>=0.6.23
openpyxl>=3.1.2
Pillow>=10.0.0
lxml>=5.0.0

# Bedrock preflight: resolving credentials, listing inference profiles and
# the one real invocation that proves the path.
boto3>=1.34.0

# PDF rendering for the visual branding check.
# On Windows this drives Word and PowerPoint through COM, which is the
# highest fidelity option. Elsewhere install LibreOffice and the scripts
# fall back to soffice.
pywin32>=306 ; sys_platform == "win32"
